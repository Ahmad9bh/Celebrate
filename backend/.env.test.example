# backend/.env.test (example) — Do NOT commit real secrets
# These values are used ONLY for local tests and prisma commands invoked by tests.
# Tests will reset the target DB schema. Use a dedicated test database/branch.

# Pooled URL for Prisma Client at runtime (vitest, dev servers)
# Example (Neon pooler host, low connection limit, ssl required):
# postgresql://user:pass@ep-xxxx-pooler.eu-west-2.aws.neon.tech/dbname?sslmode=require&pgbouncer=true&connection_limit=1&connect_timeout=15
DATABASE_URL=

# Direct (non-pooled) URL for Prisma CLI (db push/reset during tests)
# Example (Neon direct host):
# postgresql://user:pass@ep-xxxx.eu-west-2.aws.neon.tech/dbname?sslmode=require&connect_timeout=15
DIRECT_DATABASE_URL=

# Optional shadow database URL (used by Prisma migrate engine)
SHADOW_DATABASE_URL=

# Other test-related vars
JWT_SECRET=dev-secret
USE_MOCK_STRIPE=1
ALLOWED_ORIGINS=http://localhost:3000
