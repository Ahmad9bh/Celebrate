PORT=4000
JWT_SECRET=dev-secret
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
ALLOWED_ORIGINS=http://localhost:3000
## Database configuration (PostgreSQL / Neon)
# POOLED connection for runtime (Prisma Client). Keep pgbouncer=true and low connection_limit for Windows/CI.
# Example: postgresql://user:pass@ep-xxxx-pooler.us-east-1.aws.neon.tech/dbname?sslmode=require&pgbouncer=true&connection_limit=1&connect_timeout=15
DATABASE_URL=

# DIRECT connection (non-pooled) for Prisma CLI (migrate/reset/push). Use the non-pooler host from Neon.
# Example: postgresql://user:pass@ep-xxxx.us-east-1.aws.neon.tech/dbname?sslmode=require&connect_timeout=15
DIRECT_DATABASE_URL=

# Use mock Stripe client in local/dev and tests (set to 0 in staging/prod)
USE_MOCK_STRIPE=1
