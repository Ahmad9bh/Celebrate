{
  "name": "celebrate-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "dev:e2e": "cross-env NODE_ENV=test DATABASE_URL=file:./dev-test-e2e.db prisma generate && cross-env NODE_ENV=test DATABASE_URL=file:./dev-test-e2e.db prisma db push --force-reset && ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc -p .",
    "start": "node dist/server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "db:seed": "ts-node --transpileOnly prisma/seed.ts",
    "db:push": "prisma db push",
    "db:reset": "prisma migrate reset --force && prisma db seed",
    "pretest": "cross-env NODE_ENV=test DATABASE_URL=file:./dev-test.db prisma generate && cross-env NODE_ENV=test DATABASE_URL=file:./dev-test.db prisma db push --force-reset",
    "pretest:watch": "cross-env NODE_ENV=test DATABASE_URL=file:./dev-test.db prisma generate && cross-env NODE_ENV=test DATABASE_URL=file:./dev-test.db prisma db push --force-reset",
    "test": "cross-env NODE_ENV=test DATABASE_URL=file:./dev-test.db vitest run test/api.test.ts && cross-env NODE_ENV=test DATABASE_URL=file:./dev-test.db vitest run test/more.test.ts",
    "test:watch": "cross-env NODE_ENV=test DATABASE_URL=file:./dev-test.db vitest test/api.test.ts & cross-env NODE_ENV=test DATABASE_URL=file:./dev-test.db vitest test/more.test.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0",
    "celebrate": "file:..",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "next": "14.2.32",
    "stripe": "^16.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.6",
    "@types/node": "^20.11.30",
    "@types/supertest": "^6.0.2",
    "cross-env": "^7.0.3",
    "prisma": "^5.18.0",
    "supertest": "^7.0.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5",
    "vitest": "^3.2.4"
  },
  "prisma": {
    "seed": "ts-node --transpileOnly prisma/seed.ts"
  }
}
